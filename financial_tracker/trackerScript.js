const addButton = document.querySelector(".btn");


addButton.addEventListener('click', (e)=>{
    if (input.value === '' || 0) {
        alert("Пожалуйста заполните все поля, чтобы добавить операцию")
    }else{

    }
})

function addOperation() {
    const Operations = 
    `
    
    `
}